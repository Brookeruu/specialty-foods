<!DOCTYPE html>
<html lang="en">
<head>
  <title>SpecialtyFoods</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <meta name="viewport" content="width=device-width, initial-scale=1"
</head>

<body>
  <div class="container">


    <header class="container">
      <nav class="navbar navbar-default">
        <div class="navbar-header">
          <h1>Marina's Specialty Foods</h1>
          <% if user_signed_in? %>
          <p>Logged in as <strong><%= current_user.username %></strong>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete %></p>
            <% else %>
            <p> <%= link_to "Sign in", new_user_session_path %> </p>
            <% end %>
          </div>
        </nav>
        </header>
          <div class="">
            <ul>
              <li> <%= link_to "Made in USA", usa_products_path %> </li>
              <li> <%= link_to "Most Reviewed", most_reviews_products_path %> </li>
              <li> <%= link_to "Recently Added", most_recent_products_path %> </li>

            </ul>
          </div>

      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <%= yield %>
    </div>
  </body>
  </html>
